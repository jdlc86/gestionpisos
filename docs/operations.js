const map={notifications:'notificationsView',payments:'paymentsView',analytics:'analyticsView'};document.querySelectorAll('.segment').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.segment').forEach(x=>x.classList.toggle('is-active',x===b));Object.entries(map).forEach(([key,id])=>document.getElementById(id).hidden=key!==b.dataset.view)}));document.getElementById('newBroadcastBtn').addEventListener('click',()=>document.getElementById('broadcastDialog').showModal());document.getElementById('newPaymentBtn').addEventListener('click',()=>document.getElementById('paymentDialog').showModal());